package version

const MajorVersion = "1"
const MinorVersion = "0"
const PatchVersion = "0"
const AppVersion = MajorVersion + "." + MinorVersion + "." + PatchVersion + "." + BuildVersion + " [" + BuildDate + "]"
const AppName = "gokuery"
